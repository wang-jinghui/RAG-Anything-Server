# Web Framework
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22

# Database
sqlalchemy==2.0.48
asyncpg==0.31.0
psycopg2-binary==2.9.9
alembic==1.13.1
pgvector==0.4.2

# Authentication
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.1.2

# Validation & Settings
pydantic==2.11.10
pydantic-settings==2.13.1

# Rate Limiting
slowapi==0.1.9

# Async HTTP Client (for remote API calls)
aiohttp==3.13.3

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1

# ===========================================
# RAG Processing (Optional - for document processing & querying)
# ===========================================
# Core RAG library
# Note: lightrag-hku is installed from local submodule (see Dockerfile)
# lightrag-hku==1.4.11

# LLM clients (choose based on your provider)
openai==2.30.0
ollama==0.6.1

# Document parsing via API (MinerU API mode)
# Note: When using MINERU_MODE=local_api or remote, mineru package is NOT required
# Only aiohttp (above) is needed for API calls
# mineru==2.7.6  # Uncomment only if using MINERU_MODE=local (subprocess)

# Optional: Alternative parsers (only if not using MinerU API)
# docling==2.84.0
# paddleocr==3.4.0
# pypdfium2==4.30.0

# Image processing
pillow==11.3.0

# Text to PDF (for text file processing)
reportlab==4.4.10

# Markdown processing
markdown==3.10.2
weasyprint==68.1
pygments==2.19.2

# Optional: Progress bars
tqdm==4.67.3

# ===========================================
# Vector Database Clients (choose one)
# ===========================================
# PGVector (PostgreSQL with vector support)
# Already included via pgvector above

# Milvus (alternative vector DB)
# pymilvus>=2.6.2

# Qdrant (alternative vector DB)
# qdrant-client>=1.11.0

# Chroma (simple local vector DB, good for dev)
# chromadb>=0.4.0

# ===========================================
# Graph Database Clients (for knowledge graph)
# ===========================================
# Neo4j (default graph storage)
neo4j==6.1.0

# Apache AGE (PostgreSQL-based graph, alternative to Neo4j)
# age>=1.0.0

